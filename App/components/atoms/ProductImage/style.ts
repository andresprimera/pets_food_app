import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  imgContainer: {
    flex: 2,
    resizeMode: 'contain',
    justifyContent: 'center',
    paddingVertical: 2,
  },

  image: {
    width: '100%',
    height: undefined,
    aspectRatio: 1,
  },
});
